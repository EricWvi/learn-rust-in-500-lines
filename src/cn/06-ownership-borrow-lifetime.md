# 第 06 篇：所有权、借用与生命周期——borrow checker 背后真正的机制

## 一、一行签名里同时藏着两种"数据关系"

从第三篇开始，我们就一直在铺垫一件事：borrow checker 背后那套"所有权 + 生命周期"的机制。铺垫了好几篇，是时候正式介绍了。切入点还是一行我们已经很熟悉的签名——`ProcessSpawner` 的 `spawn` 方法：

```rust
fn spawn(&self, spec: ProcessSpec) -> io::Result<Self::Process>;
```

仔细看这行，它其实同时表达了两种截然不同的"和数据的关系"：

- `&self`——启动器本身只是被**借用**，调用完 `spawn`，启动器还在原地，原封不动；
- `spec: ProcessSpec`——注意没有 `&`。这份清单是**整个交出去**了：`spawn` 拿走了它的所有权，调用方之后再也用不了这份 `spec`。

一个借用，一个交出所有权，挤在同一行签名里。为什么 Rust 要把“临时借用”和“彻底交出”分得这么清楚？这套机制到底在保护什么？我们从最底层的那块基石——**所有权**——开始。

## 二、所有权（ownership）：每份数据只有一个"持有人"

Rust 内存管理的根基是一条简单的规则：**每一份数据，在任一时刻都只有唯一一个"所有者（owner）"**。你可以把所有者想成"唯一拿着钥匙的人"——钥匙只有一把，谁拿着，这份数据就归谁管。

当这个所有者"离开"（也就是它所在的作用域结束）时，这份数据会被自动**回收**——Rust 术语叫 **drop**，对应内存被释放、资源被清理。看一段最朴素的代码：

```rust
{
    let s = String::from("hi");   // s 是这份数据的所有者
}                                 // s 在这里离开作用域 -> 数据被 drop，内存释放
```

`s` 一旦走出那个 `{}`，它拥有的 `String` 立刻被回收，不需要你写任何"释放"的代码。

这一步值得和其他语言对照着看。Java、Go、JavaScript 靠**垃圾回收（GC）**：数据没人用了，由运行时在后台自动发现并清理——好处是你不用管，代价是运行时要额外开销、停顿不可预测。C 靠**手动管理**：你得自己 `malloc` / `free`——忘了 `free` 就内存泄漏，`free` 两次就崩溃，`free` 完还在用就是悬空指针。Rust 走了第三条路：**所有权 + 编译期自动 drop**——既不要 GC 的运行时开销，也不要手动管理的灾难，释放的时机在编译期就定死了（所有者离开作用域的那一刻）。

回到 `ProcessSpec`：你写 `let spec = ProcessSpec::new("echo");`，`spec` 就是这份清单的所有者；它内部的 `Vec`、`OsString` 那些，都归 `spec` 管。等 `spec` 离开作用域，Rust 会**递归地**把它内部所有东西一起 drop 掉——你不用逐个清理。

## 三、转移（move）：把钥匙交出去

既然钥匙只有一把，那"把值赋给另一个变量"或"把值传进函数"时，钥匙给谁？答案是：**给出去**。对大多数类型来说，这一步叫**转移（move）**——所有权从原来的名字"搬"到新名字，原来的名字就此作废：

```rust
let s1 = String::from("hi");
let s2 = s1;              // 所有权从 s1 转移到 s2
// println!("{}", s1);   // 编译错误（E0382）：s1 已经被 move，不能再用了
println!("{}", s2);       // s2 现在是所有者，没问题
```

`s1` 把钥匙交给了 `s2`，自己就"名存实亡"——编译器会直接拦下你对 `s1` 的任何使用，报错 `borrow of moved value`。这不是运行时检查，是编译期就堵死的。

> 一个好用的类比：**转移**像把一本书**送人**——书给出去了，你就不再拥有它；**借用**（下一节）像把书**借**给朋友——所有权还在你手里，朋友只是临时拿去看看。Rust 把这两种情况用不同的语法明确区分，恰恰是为了避免"我以为只是借你看看，你怎么把书拿走了"这种混乱。

不过，并不是所有类型都这样。第二篇讲过 `Copy` 类型——像 `i32`、`ProcessStdio` 这种小而简单的类型，赋值时是**复制**一份，而不是转移：

```rust
let a = 5;
let b = a;        // i32 是 Copy：复制一份给 b，a 还能用
println!("{} {}", a, b);
```

为什么 `i32` 能 Copy 而 `String` 不能？因为 `i32` 就是一个固定大小的数字，复制它成本极低；而 `String` 背后还连着一块堆内存，如果简单地"复制"会变成两个所有者指向同一块内存——那正是 Rust 要避免的。所以 `String` 默认转移，`i32` 默认复制。（如何让自定义类型变 `Copy`，以及 `Copy` 必须满足什么条件，等以后再细说。）

现在能回答开头的疑问了：为什么 `spawn` 的签名是 `spec: ProcessSpec`，而不是 `spec: &ProcessSpec`？因为 `spawn` 要**拿走**这份清单——它把 `spec` 里的程序名、参数、环境变量读出来，翻译成操作系统认识的命令，然后这份 `spec` 就"用完了"。把 `spec` 按值传入（move 进来），`spawn` 就成了它的所有者；等 `spawn` 结束，`spec` 被自动 drop，干净利落。调用方之后再用 `spec`，编译器直接拒绝——因为那份清单已经被"消费"掉了。

## 四、借用（borrow）：不交钥匙，只让对方"看一眼"

但很多时候你并不想把数据送出去，只是想临时给别人用。这时就用**借用（borrow）**：你保留所有权，只递给别人一个**引用（reference）**，让他顺着引用去看（或改）这份数据。引用的符号就是第三篇见过的 `&` 和 `&mut`：

- `&T`——**共享借用**：只读，可以同时有好几个，因为大家只是"看"，互不干扰。
- `&mut T`——**可变借用**：可写，但同一时刻只能有这一个，因为"改"必须独占，免得别人读到改了一半的数据。
- `*`——**解引用**：顺着 `&` 走到它指向的那个值本身（很多场景下 Rust 会自动做这步，你不用手写 `*`，但它是引用背后的基本操作）。

借用最大的好处是**不转移所有权**：

```rust
fn peek(s: &String) -> usize { s.len() }   // 借用，只读

let owned = String::from("xyz");
let len = peek(&owned);     // 只是把 owned 借给 peek 用
println!("still here: {}, len {}", owned, len);   // owned 还好好的
```

对比 `consume(s: String)`——按值传入，`owned` 被 move 走、调用后作废。**`&String` 和 `String` 这一个 `&` 之差，就是"借用"和"送出"的差别。**

第三篇我们听过那条铁律"共享不可变，可变不共享"，现在能看清它为什么是这样的了：

```rust
let mut s = String::from("hello");
let r1 = &s;                  // 共享借用
let r2 = &s;                  // 再来一个共享借用：OK，大家一起读
println!("{} {}", r1, r2);   // r1、r2 的最后一次使用
let r3 = &mut s;              // 可变借用：r1、r2 此后不再使用，借用已结束，OK
r3.push_str("!");
```

等一下，这里有个很容易卡住的细节：`r1`、`r2` 明明要到所在作用域结束才"离开"，凭什么说它们的借用"已结束"？这要归功于 Rust 的**非词法生命周期（Non-Lexical Lifetimes，NLL）**。borrow checker 判断一个借用"活多久"，看的不是"它的名字在哪一层大括号里结束"（那叫词法作用域），而是"它**最后一次被使用**在哪一行"。`r1`、`r2` 最后一次出现是在上面那行 `println!`，用过之后就再没人理它们了——所以它们的借用就在那一刻结束，下一行的 `&mut s` 自然不冲突。

> 延续前面"借书"的类比：借一本书，不是非得等你走出图书馆大门（作用域结束）才算还，而是你**放下书、不再翻它的那一刻**就算还了。下一个人（`&mut`）在你放下之后立刻就能借走，哪怕你还站在馆里没出门。

这正好能和第三篇那个被拒绝的例子对照着看。当时写的是：

```rust
let mut s = String::from("hi");
let r1 = &s;          // 只读借用
let r2 = &mut s;      // 可变借用 —— 编译器拒绝（E0502）
println!("{} {}", r1, r2);
```

区别在哪？在于 `r1` 在 `&mut` **之后**还用了一次（那行 `println!`）——它的借用还没结束，可变借用就来了，于是撞车，编译器拒绝。把"最后使用"的位置挪一挪，结果就完全不同。**NLL 让借用在"真正不再需要"的那一刻就释放，而不是死板地拖到作用域末尾**——这既让合法的代码能通过，又丝毫不放松"边读边写"那条红线。

如果允许“一边有人 `&` 在读、一边有人 `&mut` 在写”，读者就可能读到写了一半的中间状态——这正是**数据竞争**和**迭代器失效**的根源（第三篇提过的两类灾难）。铁律的存在，就是要把这种“边读边写”的冲突从根上变成不可能。

再看一个把借用和转移结合的真实例子——第四篇的 `take_stdin`：

```rust
fn take_stdin(&mut self) -> Option<Self::Stdin>;
```

它的接收者是 `&mut self`——**借用**整个把手（可变地借，但不拿走所有权），不是把把手本身 move 走。但方法要做的事，是把里面的 stdin 管道**移交给**调用方。怎么在不拿走整个把手的前提下，把里面的一部分交出去？靠的就是第四篇认识的 `Option::take`：它从 `Option` 里把值 move 出来、原地留下 `None`。于是"借走整个把手、只取走其中一根管道、把手本身还给人家"这个细腻的操作，就被干净地表达出来了。

## 五、borrow checker：它不是敌人，是替你挡刀的审稿人

前面所有规则——所有权唯一、转移后旧名作废、借用的"共享不可变可变不共享"——都不是写在文档里靠你自觉遵守的，而是被 **borrow checker（借用检查器）** 在**编译期**一条条查出来的。它在编译时扫一遍你代码里所有的所有权和借用关系，发现违规就拒绝编译。

很多人初学时觉得 borrow checker 在"找茬"，本能地想"对抗编译器"。请换个心态：**它更像一个严格但尽责的审稿人，每次拦你，多半是真的替你挡下了一个 bug。** 比如：

```rust
let s1 = String::from("hi");
let s2 = s1;
println!("{}", s1);    // 编译器拒绝（E0382）：borrow of moved value
```

`s1` 已经把所有权交给了 `s2`，你却还想用 `s1`——如果放行，运行时 `s1` 指向的内存可能已经被别人动过甚至释放了，这就是**用已释放的内存（use-after-free）**的开端。borrow checker 在编译期就把这个隐患掐了。

再比如一个想返回局部变量引用的写法，编译器也会拦下（报"缺少生命周期"或"借用了一个被释放的值"）——因为它能看出来，函数结束时那个局部变量会被 drop，返回的引用就成了**悬空指针（dangling pointer）**。C/C++ 程序员苦这些 bug 久矣；在 Rust 里，它们进不到运行时。

> 💡 所以下次 borrow checker 报错，别急着"让它闭嘴"——把它当成"我这里可能有个真 bug，让我看看是什么"。多数时候，它在阻止你写出一个将来会在凌晨三点炸掉线上服务的错误。

下面这两个例子，能让你具体感受“如果真允许边读边写，会出什么事”——以及 borrow checker 是怎么在编译期就把它们拦下的。

**例一：读着读着，被读的数据被改了**

```rust
enum StringOrInt { Str(String), Int(i64) }

let mut x = StringOrInt::Str("Hi!".to_string());
let y = &mut x;                              // 可变借用 x
if let StringOrInt::Str(ref insides) = x {   // 同时又不可变借用 x 去匹配
    *y = StringOrInt::Int(1);                // 把 x 改成 Int(1)
    println!("x says: {}", insides);         // 还想用那个“指向 String 的”引用
}
```

`insides` 是在 `x` 还是 `Str(...)` 时借出来的，它以为自己指向一个 `String`（一串“指向堆上 `Hi!` 的指针 + 长度 + 容量”）。可紧接着 `*y = Int(1)` 把 `x` 改成了 `Int(1)`——原来放 `String` 的那块内存，现在被写成了整数 `1`。等 `println!` 拿着 `insides` 去解引用时，它会把那个 `1`（以及附近的字节）当成 String 的内存地址去读——额外访问了 `1` 附近根本不存在的内存，触发 **segmentation fault**。borrow checker 不允许这种事发生：`y`（可变借用）和 `insides`（不可变借用）同时活着、还都被用到了，编译期直接拒绝（E0502）。

**例二：一边遍历、一边往里塞东西**

```rust
let mut buf = vec![1, 2, 3, 4];
for i in &buf {        // 循环不可变地借用了 buf
    buf.push(*i);      // 循环体内又可变地借用了 buf
}
```

`for i in &buf` 这一行，把 `buf` 不可变地借出去给整个循环用（`i` 是 `&i32`，借自 `buf`）。可循环体里的 `buf.push(...)` 又要可变地借 `buf`——边遍历边改，撞上铁律，编译器拒绝。

这件事在 C++ 里是能编译过去的，但后果很实在：首先，一边 `push` 一边遍历，很容易陷入**无限循环**；更要命的是，vector 满了再 `push` 会**扩容**——把整块数据搬到新地址，旧地址作废。可迭代器手里还攥着扩容前的旧地址，于是下一次访问就是 **use-after-free**。Rust 在编译期就把它拦下，根本不给它跑起来的机会。

## 六、生命周期（lifetime）：引用的"保质期"

借用还有一个无法回避的问题：**一个引用，能合法地活多久？** 答案由**生命周期（lifetime）**来管。

生命周期本质是一个**编译期的标记**，记录"这个引用至少在多长时间内是有效的"——更准确地说，是保证**引用不会比它指向的数据活得更久**。一个好用的类比：引用像一张**借书证**，数据像那家**图书馆**。借书证只在图书馆开门期间有效；图书馆关门了（数据被 drop 了），借书证就自动作废。Rust 在编译期就要证明：每一张"借书证"都不会熬到"图书馆关门"之后还在用。

为什么要费这个劲？为了从根上消灭悬空指针。看一个会失败的例子：

```rust
fn dangle() -> &String {        // 编译器拒绝（E0106）
    let s = String::from("x");
    &s                            // s 在函数结束时被 drop，这个 &s 就悬空了
}
```

函数结束时 `s` 被 drop，返回的 `&s` 指向的内存已经没了——这是教科书级的悬空指针。编译器看出来了，直接拒绝，并提示"返回类型里缺少生命周期标注"。

那真实的 `cwd_path` 为什么就合法？因为它返回的引用，是从 `self` 内部借出来的：

```rust
pub fn cwd_path(&self) -> Option<&Path> {
    self.cwd.as_deref()
}
```

那个返回的 `&Path` 指向 `self` 里的字段，只要 `self` 还活着，它就有效。第四篇我们说过"`&Path` 不能比 `self` 活得更久"——现在有了名字：这就是**生命周期绑定**。你看签名里并没把生命周期写出来，那是编译器按**省略规则（elision）**帮你补上了：像 `fn f(&self) -> &T` 这种"从 `&self` 借出 `&T`"的常见模式，编译器默认把返回引用的生命周期绑定到 `self` 上，不用你手写。

当省略规则不够用、需要你亲自说明“哪个引用活多久”时，就得**显式标注**生命周期。语法是用一个带撇号的标识符，比如 `'a`：

```rust
fn first<'a>(s: &'a str) -> &'a str { /* ... */ }
```

这里的 `'a` 是一个**生命周期参数**——和泛型参数（第三篇的 `<T>`）是同一套思路，只不过泛型参数说的是“装什么类型”，生命周期参数说的是“引用活多久”。`fn first<'a>(s: &'a str) -> &'a str` 的意思是：“返回的引用，和传入的 `s` 活得一样久。”

> 你可能会卡在这里：`first` 不也就一个输入引用吗？上面 `cwd_path(&self) -> &Path` 同样只有一个输入引用，它没写 `'a` 也过了，凭什么 `first` 要写？——问得好，其实**它也不用写**。省略规则里有一条：当函数**只有一个**输入引用时，编译器默认返回的引用就借自它。所以 `fn first(s: &str) -> &str` 这样写完全能编译通过；上面那行带 `'a` 的版本，只是把省略规则背后补的东西“展开”给你看，让你看清 `'a` 长什么样。`cwd_path` 走的也是同一条规则（它唯一的输入引用就是 `&self`），所以同样不用手写。

那到底什么时候**必须**手写 `'a`？当输入引用**不止一个**、编译器判断不出返回值借自哪一个时。看这个返回较长字符串的函数：

```rust
// 两个输入引用，编译器拒绝（E0106）：返回的 &str 到底借自 x 还是 y？
fn longer(x: &str, y: &str) -> &str {
    if x.len() >= y.len() { x } else { y }
}
```

两个输入 `&str`、输出也是 `&str`，但“输出到底跟 `x` 还是跟 `y` 绑定”编译器猜不出——也许你借自 `x`、也许借自 `y`，两者存活时长未必相同。这时省略规则失效，你必须亲自声明：

```rust
fn longer<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() >= y.len() { x } else { y }
}
```

`'a` 在这里同时出现在三个位置，意思是“两个输入引用和返回引用，共享同一个生命周期 `'a`”——即要求 `x`、`y` 活得一样久，返回值也跟着活那么久。这样编译器才有足够的信息检查。（方法里有个特例：如果输入引用有多个、但其中一个是 `&self` / `&mut self`，编译器会默认返回值借自 `self`，签名照样能省略 `'a`——前提是返回值确实借自 `self`。）

最后补一句之前埋过的 `'static`（第五篇见到的第一个具体生命周期）：它是一个**特殊的生命周期**，表示“活到整个程序结束”——也就是不借用任何短命的数据。字符串字面量就是 `&'static str`，因为它们直接编进了程序里，永远有效。更复杂的多生命周期情况（比如两个输入各有各的生命周期、要分别标注 `'a`、`'b`），我们以后再展开。

## 七、串起来：spawn 这一行的全景

现在我们重新读一遍开头那行签名，每个部分都应该说得通了：

```rust
fn spawn(&self, spec: ProcessSpec) -> io::Result<Self::Process>;
```

- `&self`——**借用**启动器：只读地借用，不拿走所有权，调用完启动器原样还在。
- `spec: ProcessSpec`——**按值传入，即转移**：`spawn` 拿走这份清单的所有权，读出里面的配置翻译成 OS 命令，函数结束时 `spec` 被 drop。调用方之后再用 `spec` 会被编译器拒绝。
- `Self::Process`——返回一个**拥有所有权**的进程把手：所有权从 `spawn` 转移给调用方。

一行签名，把"借用"和"转移"两种关系交代得清清楚楚——而这正是 Rust 用类型和符号把内存关系写在明面上的体现。

## 八、小结

- **所有权（ownership）**：每份数据任一时刻只有一个所有者；所有者离开作用域时数据被自动 **drop**。Rust 既不要 GC 的运行时开销，也不要手动管理的泄漏与悬空，释放时机编译期定死。
- **转移（move）**：对非 `Copy` 类型，赋值或传参会把所有权**搬走**，旧名作废（E0382）。`Copy` 类型（如 `i32`）则是复制。`spawn` 按 `spec: ProcessSpec` 传值，就是要"拿走并消费"这份清单。
- **借用（borrow）**：`&T` 共享只读（可多个）、`&mut T` 独占可写（只能一个）、`*` 解引用。借用不转移所有权。铁律"共享不可变，可变不共享"是为了让"边读边写"的数据竞争/迭代器失效从根上不可能。借用的结束看**最后一次使用**而非作用域末尾（NLL）。`take_stdin(&mut self)` 借走把手、用 `Option::take` 把管道 move 出来，是借用与转移的精妙结合。
- **borrow checker** 是编译期执行上述检查的组件；把它当尽责的审稿人，而非敌人——它拦下的多是 use-after-free、悬空指针、数据竞争这类真 bug。
- **生命周期（lifetime）** 是引用的"保质期"，保证引用不比数据活得久（借书证 vs 图书馆）。`cwd_path(&self) -> &Path` 的返回引用生命周期绑定到 `self`（通常由 elision 省略）；不够时用 `'a` 显式标注，如 `fn f<'a>(s: &'a str) -> &'a str`。`'static` 是活到程序结束的特殊生命周期。
